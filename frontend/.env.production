# Production builds call the deployed gateway directly (requires CORS on the gateway).
# Remove/override this to fall back to the /api proxy (see vite.config.js).
VITE_API_BASE=http://13.250.9.139:5000
