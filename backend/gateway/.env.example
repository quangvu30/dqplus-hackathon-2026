PORT=3000
HOST=0.0.0.0
# Comma-separated list of allowed CORS origins.
# Defaults to dqplus.ddns.net and localhost (any scheme/port).
# CORS_ORIGIN=http://dqplus.ddns.net,http://localhost:5173

DB_HOST=localhost
DB_PORT=5432
DB_NAME=dealflow
DB_USER=dqplus
DB_PASSWORD=hackathondqplus
DB_SSL=false

JWT_SECRET=change-me-in-a-real-environment
JWT_EXPIRES_IN=1d

EXTRACT_SERVICE_URL=http://localhost:3001
